{
  "name": "shopify-ec-manager",
  "version": "1.0.0",
  "description": "EC店長 - Shopify Store Management System",
  "main": "index.js",
  "scripts": {
    "setup": "node scripts/setup.js",
    "verify": "node scripts/verify_pipeline.js",
    "verify:secrets": "node scripts/verify_secrets.js",
    "deploy:test": "shopify theme push --path=theme --env=test",
    "deploy:prod": "shopify theme push --path=theme --env=prod",
    "lighthouse": "npx @lhci/cli@0.16.x autorun --collect.url=https://${SHOP_URL}",
    "test": "jest",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "openai": "^4.17.0"
  },
  "devDependencies": {
    "@lhci/cli": "^0.16.0",
    "@playwright/test": "^1.40.0",
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/your-org/shopify-ec-manager.git"
  },
  "keywords": [
    "shopify",
    "ecommerce",
    "automation",
    "ai",
    "openai"
  ],
  "author": "EC店長 Team",
  "license": "MIT"
}
